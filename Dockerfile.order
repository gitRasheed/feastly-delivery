# Use pre-built JAR for faster builds
# Run: ./gradlew :order-service:bootJar before docker compose up
FROM eclipse-temurin:21-jre-alpine
WORKDIR /app
COPY order-service/build/libs/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
